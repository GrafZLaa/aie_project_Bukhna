# notebooks/

| Ноутбук | Что внутри |
|---|---|
| `01_eda.ipynb`        | разбор корпуса PDF — размеры, число страниц, embedded-текст vs скан, типы документов, заполненность полей в эталоне |
| `02_baselines.ipynb`  | сравнение baseline (OCR + правила) и improved (+ LLM) на контрольной выборке. Таблицы, графики, живой прогон одного PDF |

Логика OCR/правил/LLM/eval живёт в `app/app.py` и доступна через `src/`. Ноутбуки её только зовут, не дублируют.
