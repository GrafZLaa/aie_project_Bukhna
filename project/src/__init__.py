"""Логический пакет проекта.

Исторически весь backend собран в одном файле ``app/app.py`` (наследие из
хакатона). Подпакеты ``src/`` обозначают логические слои пайплайна и
реэкспортируют соответствующие функции из ``app/app.py``:

- :mod:`src.data`    — рендер PDF в изображения и извлечение embedded-текста
- :mod:`src.ocr`     — OCR (Tesseract) и предобработка изображений
- :mod:`src.rules`   — regex/правила извлечения полей и нормализация
- :mod:`src.llm`     — опциональная LLM-доскан-обработка через Ollama
- :mod:`src.service` — Flask-приложение и HTTP-эндпойнты
- :mod:`src.eval`    — оценка качества на контрольной выборке
"""
